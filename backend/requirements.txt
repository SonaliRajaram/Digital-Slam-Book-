fastapi==0.111.0
uvicorn[standard]==0.30.1
sqlalchemy==2.0.30
pydantic[email]==2.7.1
python-jose[cryptography]==3.3.0
passlib==1.7.4
bcrypt==4.0.1
python-multipart==0.0.9

# Only required if STORAGE_PROVIDER=cloudinary in production.
# Not needed for local development (LocalStorage doesn't import it).
cloudinary==1.40.0

# PDF generation for the export feature
reportlab==4.2.0
